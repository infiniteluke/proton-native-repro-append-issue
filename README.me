# proton-native append issue

## How to reproduce
* `yarn`
* `yarn start`
* Click add a few times
* Click remove
* Notice the Picker/RadioButton items, which are driven by state are not reset, but the count is